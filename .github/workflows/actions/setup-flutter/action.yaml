name: Setup Flutter
description: Setup Flutter environment with dependencies

runs:
    using: composite
    steps:
        - name: Change directory to flutter
          shell: bash
          run: cd flutter
        - uses: subosito/flutter-action@v2
          with:
              flutter-version: "3.41.6"
              channel: "stable"
              cache: true
        - name: Install dependencies
          shell: bash
          run: flutter pub get
